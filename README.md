# es6dev-template
don't forget to change your project name in `package.json`

## Development 

    $ npm install
    $ npm run serve

##  Production

    $ npm install -g forever
    $ forever dist/index.js
